spring.application.name=quizService

server.port= ${QUIZ_SERVICE_PORT}

# Eureka client settings: point to the Eureka server
eureka.client.service-url.defaultZone=http://localhost:${REGISTERYSERVERPORT}/eureka

spring.datasource.url= jdbc:postgresql://localhost:5432/quizdb
spring.datasource.username=postgres
spring.datasource.password=mahsan
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect